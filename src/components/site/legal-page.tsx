import Link from "next/link";
import { AlertCircle, ArrowLeft } from "lucide-react";
export function LegalPage({ eyebrow, title, status, intro, sections }: { eyebrow: string; title: string; status: string; intro: string; sections: [string, string][] }) {
  return <><header className="legal-hero"><div className="shell legal-hero__inner"><Link className="back-link" href="/"><ArrowLeft size={17} /> Back to home</Link><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p>{intro}</p><span><AlertCircle size={17} />{status}</span></div></header><div className="shell legal-layout"><aside><strong>On this page</strong>{sections.map(([heading]) => <a key={heading} href={`#${heading.toLowerCase().replaceAll(" ", "-")}`}>{heading}</a>)}</aside><div>{sections.map(([heading, body]) => <section key={heading} id={heading.toLowerCase().replaceAll(" ", "-")}><h2>{heading}</h2><p>{body}</p></section>)}<div className="notice-box"><strong>Approval required before launch</strong><p>This page intentionally identifies missing legal information instead of fabricating it. Replace the draft with counsel-approved content once the operating entity and production services are confirmed.</p></div></div></div></>;
}
